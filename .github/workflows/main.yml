name: Demo Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Tulosta tervehdys
        run: echo "👋 Hei GitHub Actionsista, Mike!"

      - name: Tulosta päivämäärä
        run: date
